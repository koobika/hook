Build the app for linux:
	bash build.sh # it will leave httpserver in you current directory
	To run the app, you need to install musl (apt-get install musl)
To run the test just type:
go build
go run httpserver.go

